---
interface Props {
  items: string[];
  label?: string;
}
const { items, label = 'Exposure data' } = Astro.props;
---

{
  items.length > 0 && (
    <ul class="exif" aria-label={label}>
      {items.map((item) => (
        <li>{item}</li>
      ))}
    </ul>
  )
}
